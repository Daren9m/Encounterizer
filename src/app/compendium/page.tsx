import ReferenceLibrary from '@/components/ReferenceLibrary';

/** Legacy deep link retained for existing bookmarks and shared URLs. */
export default function CompendiumPage() {
  return <ReferenceLibrary />;
}
